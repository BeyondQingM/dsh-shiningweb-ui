/** 视觉主题应用：主题色/毛玻璃写入 document.documentElement CSS 变量。 */
import type { ShiningSettings } from '../settings.ts';
/** 将设置投影到 CSS 变量（浏览器环境调用）。 */
export declare function applyVisual(settings?: ShiningSettings): void;
